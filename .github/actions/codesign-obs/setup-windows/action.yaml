name: Setup Windows Code Signing
description: Sets up code siging for Windows
inputs:
  gcp-identity-provider:
    description: GCP Identity Provider Pool ID
    required: true
  gcp-account-name:
    description: Google service account name
    required: true
  github-token:
    description: The GitHub token used to make authenticated API requests.
    default: ${{ github.token }}
runs:
  using: composite
  steps:
    - name: Check Runner Operating System
      if: runner.os != 'Windows'
      shell: bash --noprofile --norc -eo errexit -eo pipefail -eo nounset {0}
      run: |
        : Check Runner Operating System
        echo '::error::codesign-obs/setup-windows action requires a Windows-based runner.'
        exit 1

    - name: Setup Google CNG Provider
      id: setup-gcp
      shell: pwsh -command ". '{0}'"
      env:
        CNG_VERSION: 'cng-v1.4'
        GH_TOKEN: ${{ inputs.github-token }}
      run: . ${env:GITHUB_ACTION_PATH}/Setup-GCP.ps1

    - name: 'Authenticate to Google Cloud'
      uses: 'google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093' # v3.0.0
      with:
        workload_identity_provider: ${{ inputs.gcp-identity-provider }}
        service_account: ${{ inputs.gcp-account-name }}
